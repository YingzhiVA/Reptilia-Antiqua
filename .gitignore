*:Zone.Identifier

# local backlog/notes, not part of the repo history
BACKLOG.md

# local Python venv + bytecode cache for the image pipeline (tools/embed_image.py)
tools/venv/
__pycache__/

# raw artwork drops awaiting embedding (the embedded + art/ copies are what we keep)
Gemini_Generated_*.png
